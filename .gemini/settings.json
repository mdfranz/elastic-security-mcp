{
  "mcpServers": {
    "elastic-security": {
      "command": "./elastic-mcp-server",
      "args": [],
      "env": {
        "ELASTIC_URL": "${ELASTIC_URL}",
        "ELASTIC_KEY": "${ELASTIC_KEY}"
      }
    }
  }
}
